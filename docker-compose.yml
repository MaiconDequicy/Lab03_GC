version: '3'
services:
  app:
    image: maiconcostadeq821/webacademy:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production